ref="index.html"
import resume from '../Resumepdf'

var typed = new typed(".text",
    {
    strings:["Frontend Developer", "Web Developer", "Online Tutor"],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
})
